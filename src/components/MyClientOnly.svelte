<script>
  // Calling alert only works on the client-side
  // because it assumes window is the global object.
  alert('This is from MyClientOnly!');
</script>

<p>This component only loads on the client-side.</p>
